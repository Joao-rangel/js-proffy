import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

/* criar app: yarn create react-app web --template typescript | web = nome */

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
